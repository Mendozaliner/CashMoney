# Session 21 — 2026-07-24 (second run of day)

**Phase 2 — PROVE** (champion v2 frozen; live clock week 2 of ~13).
Zero-config engineering + literature session: s20 closed the last SPY-overlay
families and concluded the agenda "must look beyond single-index SPY overlays."
The only open theory family is #6 Value tilt — blocked on fundamentals data.
This session unblocks it.

## Scoreboard (mark CARRIED — no new completed close since s20)

| Item | Value | vs SPY (day) | vs SPY (since live) |
|---|---|---|---|
| Portfolio | **$984.35** | — (carried) | −1.565% vs −1.467% |
| SPY latest usable close | $738.18 (2026-07-23) | — | — |
| Peak / live worst dip | $1,006.52 / **−2.20%** (GREEN) | — | — |

Holdings: 1.333476 SPY. v2 exposure 1.0000. No trades. Data manifest CURRENT
(stale_days=0, 43 tickers); today's partial 2026-07-24 bar dropped per protocol.
Guardrails G1–G7 **ALL GREEN**. live_track: 0 completed months; worst live DD −2.20%;
MinTRL vs SPY now FINITE at ~123 trading days (was infinite in s19 — the −1.23% SPY
day on 07-23 created the first live tracking dispersion).

## Research — valuation (CAPE) literature pass (8 sources; 0 configs; nothing registered)

Notes: `research/2026-07-24-s21-value-timing-literature.md`. Key findings:
- CAPE↔1-yr returns correlation ≈ 0 (Kitces, Dimensional): useless as a short-horizon switch.
- Asness 2012: decade-horizon expected-return signal is real (10-yr forward returns fall
  near-monotonically with starting CAPE) — usable, but only as a *mild tilt*.
- AQR "Sin a Little": value timing works best combined with momentum, sized modestly —
  maps directly onto tilting v2's exposure rather than replacing its gate.
- Decile studies: signal has teeth only in the extreme top decile (CAPE > ~28); currently
  CAPE ≈ 41 (top decile), so the overlay would be LIVE-relevant if adopted.
- Honest prior: E29/E30 failed at corr≈0.97 to v2. CAPE is genuinely orthogonal to
  price/vol — the one reason this family merits its alpha spend. Default expectation
  remains FAIL (overlay inactive ~90% of history → CI will struggle to clear zero).

## Engineering shipped (zero configs)

- `scripts/fetch_data.py`: `fetch_shiller()` + `refresh_fundamentals()` — Shiller
  monthly S&P dataset (Price/Dividend/Earnings/CPI/CAPE, 1871→) via Yale `ie_data.xls`,
  datahub CSV fallback → `data/cache/fundamentals/shiller_monthly.csv`; manifest gains
  `fundamentals_ok`.
- `.github/workflows/update-data.yml`: installs from `scripts/requirements-data.txt`
  (adds xlrd/openpyxl), commits `data/cache/fundamentals/`. Push-trigger means the
  first fundamentals file lands with this session's push.
- `data/loader.py`: `load_shiller()` with explicit no-lookahead guidance (publication
  lag ⇒ shift(1); expanding-window percentiles only).
- `tests/test_shiller.py`: 5 offline parser/loader tests (fractional-year date
  disambiguation incl. the `.1`=October quirk, no-ffill, loader round-trip).
  **Suite 106/106** (was 101).
- `research/run_2026_07_24_s21_mark.py`: carried-mark verification script.

## Graduation scoreboard (unchanged)

1. Beats S&P risk-adjusted OOS, significantly: ✗ — CI [−0.0128,+0.7077]; re-check early Aug
2. Live 3-mo outperformance: ✗ — 0/3 months; first checkpoint ~2026-08-13
3. Max drawdown < 20%: ✓ — live −2.20%; worst backtest fold −20.5%
4. Full costs: ✓

## Keep/revert

Champion v2 UNCHANGED. No experiments run (engineering session — precedent s14/s16/s17/s19).
Configs unchanged at **200**.

## Roadmap (top 3)

1. **E31 CAPE value tilt on v2** — next research session, once
   `shiller_monthly.csv` lands (likely today via push-trigger). Pre-register FIRST:
   mild tilt, ≤2 params, expanding-window percentile, shift(1), ≤12 configs.
2. **Live-track marks** — first monthly checkpoint ~2026-08-13 (Criterion 2). HIGHEST PRIORITY.
3. **v2 significance re-check** (#10) — first session of August.
