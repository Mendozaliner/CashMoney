# assets

Drop the firm logo here as **`logo.png`** (exact path: `assets/logo.png`).

Once it exists, the daily email briefing automatically uses it at the top of
the signature (rendered ~220px wide) instead of the text monogram. For best
results in email clients: ~440px wide, transparent or white background.
